# BankAccountManager-C

A simple banking management system in C for registering, sorting, searching, and updating bank accounts.  
Includes file handling to store account data.

## Compile & Run

```bash
gcc main.c -o bankapp
./bankapp
